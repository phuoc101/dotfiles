# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:CaskaydiaCove Nerd Font 12

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
# exec --no-startup-id xss-lock --transfer-sleep-lock -- betterlockscreen -l dimblur

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Border
# default_border pixel 4
for_window [class=".*"] border pixel 0

# Set inner/outer gaps
gaps inner   8
gaps outer   4
gaps top     4
gaps bottom  4
# smart_borders on

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status pkill -SIGRTMIN+10 i3blocks
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status 
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause

# Toggle border
bindsym $mod+Shift+b border toggle

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

### App shortcuts ###

bindsym $mod+Escape exec betterlockscreen -l dimblur

# start a terminal
# bindsym $mod+t exec i3-sensible-terminal; workspace $ws1
bindsym $mod+t exec kitty; workspace $ws1

# start a browser
bindsym $mod+b exec firefox; workspace $ws2

# start a browser
bindsym $mod+f exec nautilus

# screenshot
bindsym Print exec flameshot gui

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec --no-startup-id dmenu_run
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# start rofi (program launcher)
set $ROFI_SCRIPT_DIR ~/.config/rofi/scripts
bindsym $mod+d exec rofi -show drun -show-icons
bindsym $mod+Tab exec rofi -show window -show-icons
bindsym $mod+Shift+C exec rofi -show calc -modi calc -no-show-match -no-sort
bindsym $mod+shift+P exec zsh $ROFI_SCRIPT_DIR/system.sh
# workspace navigation
bindsym $mod+Control+j workspace next
bindsym $mod+Control+k workspace prev
bindsym $mod+period exec rofimoji

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
set $window-move-amount 100px
bindsym $mod+Shift+h move left $window-move-amount
bindsym $mod+Shift+j move down $window-move-amount
bindsym $mod+Shift+k move up $window-move-amount
bindsym $mod+Shift+l move right $window-move-amount

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Return split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+m fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+s layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+g floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1:  "
set $ws2 "2:  "
set $ws3 "3:  "
set $ws4 "4:  "
set $ws5 "5: 犯 "
set $ws6 "6:  "
set $ws7 "7:  "
set $ws8 "8:  "
set $ws9 "9:  "
set $ws10 "10:  "
set $ws11 "11: 﫯 "
set $ws12 "12"
set $ws13 "13"
set $ws14 "14"
set $ws15 "15"
set $ws16 "16"
set $ws17 "17"
set $ws18 "18"
set $ws19 "19:  "
set $ws20 "20:  "

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
bindsym $mod+F1 workspace $ws11
bindsym $mod+F2 workspace $ws12
bindsym $mod+F3 workspace $ws13
bindsym $mod+F4 workspace $ws14
bindsym $mod+F5 workspace $ws15
bindsym $mod+F6 workspace $ws16
bindsym $mod+F7 workspace $ws17
bindsym $mod+F8 workspace $ws18
bindsym $mod+F9 workspace $ws19
bindsym $mod+F10 workspace $ws20

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10
bindsym $mod+Shift+F1 move container to workspace $ws11
bindsym $mod+Shift+F2 move container to workspace $ws12
bindsym $mod+Shift+F3 move container to workspace $ws13
bindsym $mod+Shift+F4 move container to workspace $ws14
bindsym $mod+Shift+F5 move container to workspace $ws15
bindsym $mod+Shift+F6 move container to workspace $ws16
bindsym $mod+Shift+F7 move container to workspace $ws17
bindsym $mod+Shift+F8 move container to workspace $ws18
bindsym $mod+Shift+F9 move container to workspace $ws19
bindsym $mod+Shift+F10 move container to workspace $ws20

# reload the configuration file
# bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+Q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# language
# bindsym $mod+shift+e exec ibus engine xkb:us::eng
# bindsym $mod+shift+f exec ibus engine xkb:fi::fin
# bindsym $mod+shift+v exec ibus engine Unikey
bindsym $mod+shift+space exec zsh ~/.zsh/toggle_language.zsh

# resize window (you can also use the mouse for that)
set $window-resize-amount 20px
mode "resize" {
# These bindings trigger as soon as you enter the resize mode

# Pressing left will shrink the window’s width.
# Pressing right will grow the window’s width.
# Pressing up will shrink the window’s height.
# Pressing down will grow the window’s height.
    bindsym h resize shrink width $window-resize-amount or 1 ppt
    bindsym k resize shrink height $window-resize-amount or 1 ppt
    bindsym l resize grow width $window-resize-amount or 1 ppt
    bindsym j resize grow height $window-resize-amount or 1 ppt

# same bindings, but for the arrow keys
    bindsym Left resize shrink width $window-resize-amount or 1 ppt
    bindsym Down resize grow height $window-resize-amount or 1 ppt
    bindsym Up resize shrink height $window-resize-amount or 1 ppt
    bindsym Right resize grow width $window-resize-amount or 1 ppt

# back to normal: Enter or Escape or $mod+r
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+Shift+Control+h resize shrink width $window-resize-amount or 1 ppt
bindsym $mod+Shift+Control+k resize shrink height $window-resize-amount or 1 ppt
bindsym $mod+Shift+Control+l resize grow width $window-resize-amount or 1 ppt
bindsym $mod+Shift+Control+j resize grow height $window-resize-amount or 1 ppt
bindsym $mod+Shift+Control+Left resize shrink width $window-resize-amount or 1 ppt
bindsym $mod+Shift+Control+Up resize shrink height $window-resize-amount or 1 ppt
bindsym $mod+Shift+Control+Right resize grow width $window-resize-amount or 1 ppt
bindsym $mod+Shift+Control+Down resize grow height $window-resize-amount or 1 ppt

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
  status_command i3blocks

    status_command SCRIPT_DIR=~/.config/i3blocks i3blocks
    position top

# please set your primary output first. Example: 'xrandr --output eDP1 --primary'
# alternative: tray_output eDP1
    tray_output primary
    tray_padding 3

    bindsym button4 nop
    bindsym button5 nop
    font xft:CaskaydiaCove Nerd Font 12
# strip_workspace_numbers yes

    colors {
      background #1E1E2E
        statusline #BFBDB6
        separator  #81A1C1

#                  border  backgr. text
        focused_workspace  #F9E2AF #F9E2AF #1F2531
        active_workspace   #81A1C1 #1F2531 #ECEFF4
        inactive_workspace #45475a #45475A #ECEFF4
        binding_mode       #D08770 #D08770 #ECEFF4
        urgent_workspace   #BF616A #BF616A #ECEFF4
    }
}

# disable focus on mouse hover
focus_follows_mouse no

### Startup executions ###
# auto mount hard disks
exec --no-startup-id udiskie

# Configure multi-monitor
exec --no-startup-id /home/phuoc101/.zsh/display_init.zsh
# start language daemon
exec --no-startup-id ibus-daemon --xim -d -r
# start syncthing
exec --no-startup-id /usr/bin/syncthing serve --no-browser --logfile=default
# start clipboard manager
exec --no-startup-id copyq

# start picom
# exec --no-startup-id picom
# exec --no-startup-id ~/sys_tools/xborder/xborders -c ~/.config/xborder/config.json
set $I3_DIR ~/.config/i3
exec_always --no-startup-id $I3_DIR/reset_picom.zsh

### Workspace assignment ###
assign [class="kitty"] $ws1
assign [class="Alacritty"] $ws1
assign [class="Gnome-terminal"] $ws1

assign [class="firefox"] $ws2
assign [class="brave-browser"] $ws2

assign [class="Signal"] $ws3
for_window[class="Signal"] focus
assign [class="TelegramDesktop"] $ws3
for_window[class="TelegramDesktop"] focus

assign [class="discord"] $ws4
for_window[class="discord"] focus

assign [class="zoom"] $ws5
for_window[class="zoom"] focus

assign [class="Io.github.celluloid_player.Celluloid"] $ws6
for_window[class="Io.github.celluloid_player.Celluloid"] focus
assign [class="mpv"] $ws6
for_window[class="mpv"] focus
assign [class="vlc"] $ws6
for_window[class="vlc"] focus

assign [class="MATLAB"] $ws7
for_window[class="MATLAB"] focus
assign [class="Code"] $ws7
for_window[class="Code"] focus
assign [class="QtCreator"] $ws7
for_window[class="QtCreator"] focus

assign [class="wps"] $ws8
for_window[class="wps"] focus
assign [class="wpp"] $ws8
for_window[class="wpp"] focus

assign [class="teams"] $ws9
for_window[class="teams"] focus
assign [class="Slack"] $ws9
for_window[class="Slack"] focus

assign [class="Blender"] $ws10
for_window[class="Blender"] focus

assign [class="thunderbird"] $ws11
for_window[class="thunderbird"] focus

assign [class="obsidian"] $ws19
for_window[class="obsidian"] focus

assign [class="obs"] $ws20
for_window[class="obs"] focus

### Monitor config ###
set $lap_monitor EDP-1-1
set $ext_monitor HDMI-0

# bind workspaces to monitor
workspace $ws1 output $ext_monitor
workspace $ws2 output $ext_monitor
workspace $ws3 output $ext_monitor
workspace $ws4 output $ext_monitor
workspace $ws5 output $ext_monitor
workspace $ws6 output $ext_monitor
workspace $ws7 output $ext_monitor
workspace $ws8 output $ext_monitor
workspace $ws9 output $ext_monitor
workspace $ws10 output $ext_monitor
workspace $ws11 output $ext_monitor
workspace $ws12 output $ext_monitor
workspace $ws13 output $ext_monitor
workspace $ws14 output $ext_monitor
workspace $ws15 output $lap_monitor
workspace $ws16 output $lap_monitor
workspace $ws17 output $lap_monitor
workspace $ws18 output $lap_monitor
workspace $ws19 output $lap_monitor
workspace $ws20 output $lap_monitor

#Theme
set $bgcolor #2e3440
set $ibgcolor #3b4252
set $textcolor #ECEFF4
set $bordercolor #FFAD66
set $indicolor #ECEFF4
set $itextcolor #4c566a
set $ubgcolor #bf616a

#                           border        background    text            indicator
client.focused              $bordercolor  $bordercolor  $bgcolor        $indicolor
client.unfocused            $ibgcolor     $ibgcolor     $indicolor      $ibgcolor
client.focused_inactive     $ibgcolor     $ibgcolor     $indicolor      $ibgcolor
client.urgent               $ubgcolor     $ubgcolor     $textcolor      $ubgcolor
